# Password-Manager
A secure Password Manager application using React for the frontend and MongoDB for backend storage. It allows users to safely store, manage, and retrieve their passwords in an encrypted format. The app provides an easy-to-use interface for adding, editing, and deleting credentials, along with secure authentication to ensure data privacy
